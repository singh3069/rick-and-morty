/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  images: {
    domains: ["rickandmortyapi.com"],
  },
};

// const nextConfig = {
//   reactStrictMode: true,
// };

// module.exports = nextConfig;
